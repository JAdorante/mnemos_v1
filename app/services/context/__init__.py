"""CAL — the Context Association Layer.

Stage 0 (seeding) ships `keys`: deterministic extraction of context keys from
observable signals. Later stages add binding lookup, frames, the resolver and
episodes; nothing here imports a model, and nothing here touches the network.
"""
